spring:
  datasource:
    url: jdbc:mysql://localhost:3306/insurance_db
    username: root
    password: zero
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
